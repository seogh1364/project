# 작업내역

작업일: 2026-05-27

## 작업 기준

- 최신 `origin/main`에서 만든 `feature/css-js-split-main` 브랜치를 기준으로 작업했습니다.
- 코스 정보는 JSON/API가 아니라 `index.html`의 HTML 카드에 직접 입력하는 방식으로 유지했습니다.
- 화면 전환은 `index.html` 안의 섹션을 JavaScript가 숨김/표시하는 SPA 방식으로 유지했습니다.
- 상세 가이드 하단은 지도 API가 아니라 이미지 1장을 연결하는 영역으로 유지했습니다.

## 변경한 파일

### `index.html`

- HTML 안의 inline `<style>`과 inline `<script>`를 제거하고 `main.css?v=20260527`, `app.js?v=20260527`만 연결했습니다.
  - 이유: HTML은 구조, CSS는 스타일, JS는 동작을 각각 담당하게 분리하기 위해서입니다.
- 최신 main에 있던 `성산 산책`, `홍제천 산책 데이트`, 연남동 코스들과 `home`, `courses`, `result`, `saved` 화면 구조를 복구했습니다.
  - 이유: OneDrive의 `index.html`을 파일 단위로 병합하면서 최신 main의 코스 코드가 덮어써졌기 때문입니다.
- OneDrive 파일에 있던 서교동 결과 카드 2개를 결과 화면에 다시 반영했습니다.
  - 이유: 사용자가 합치라고 준 HTML의 코스 내용을 보존하기 위해서입니다.
- 각 코스 카드에 `data-course-card`, `data-course-id`, `data-save-course`, `data-guide-open` 훅을 유지했습니다.
  - 이유: JS 담당자가 코스 카드를 읽어 저장 기능과 상세 가이드를 연결하기 쉽게 하기 위해서입니다.
- 상세 가이드 하단을 `.guide-image-preview` 이미지 영역으로 정리했습니다.
  - 이유: 지도 API 대신 이미지 1장을 연결하기로 결정했기 때문입니다.

### `main.css`

- 기존 main의 CSS를 `index.html`에서 분리해 `main.css`로 옮겼습니다.
  - 이유: GitHub Pages 배포 시에도 HTML/CSS 역할을 분리해서 관리하기 위해서입니다.
- 상세 가이드 이미지 영역용 `.guide-image-preview`, `.guide-detail-image` 스타일을 유지했습니다.
  - 이유: 상세 가이드가 기존 사이트 색상 배합 안에서 이미지 영역을 안정적으로 보여주게 하기 위해서입니다.

### `app.js`

- 화면 전환, 목록 메뉴, 저장 목록, 상세 가이드 모달 동작을 `app.js`로 분리했습니다.
  - 이유: HTML에서 JavaScript를 빼서 JS 담당자가 동작 파일만 보고 수정할 수 있게 하기 위해서입니다.
- 저장 기능은 HTML 카드 내용을 읽어서 `localStorage`에 저장하도록 유지했습니다.
  - 이유: 현재는 서버 없이 GitHub Pages로 배포할 예정이라 브라우저 안에서 저장되는 구조가 가장 단순하기 때문입니다.
- 상세 가이드는 클릭한 코스 카드의 제목, 예산, 시간, 이동 방식, 코스 단계를 읽어 자동으로 바뀌게 했습니다.
  - 이유: 새 코스를 추가할 때 JS에 코스 데이터를 다시 적지 않아도 HTML 카드만 추가하면 같은 형식으로 동작하게 하기 위해서입니다.

## 복구한 내용

- 사라졌던 `성산 산책`, `홍제천 산책 데이트` 코드는 최신 `origin/main`에 원래 있던 내용이 맞습니다.
- 원인은 OneDrive의 `index.html`이 최신 main보다 오래된 구조라, 그 파일을 넓게 반영하면서 최신 main의 코스 목록이 덮어써진 것입니다.
- 지금은 최신 `origin/main:index.html`을 기준으로 다시 잡고, CSS/JS 분리와 OneDrive에서 필요한 서교동 카드만 다시 반영했습니다.

## 다음 담당자 참고

- 새 코스는 같은 HTML 카드 형식으로 `index.html`에 추가하면 됩니다.
- 코스별 상세 가이드는 JS가 카드 안의 `.path-node`, `.price-tag`, `.card-info`를 읽어서 자동 구성합니다.
- 코스별로 다른 상세 이미지를 쓰려면 해당 카드에 `data-guide-image="이미지파일명.png"`를 추가하면 됩니다.
- 현재 저장 기능은 같은 브라우저 안에서만 유지됩니다. 로그인 사용자 간 공유 저장이 필요하면 이후 Firebase/Supabase 같은 외부 저장소로 바꾸면 됩니다.

## 추가 작업 - CSS 파일 방식과 Tailwind class 방식 분리

- `index-css.html`을 추가했습니다.
  - 이유: 현재 상태처럼 `index.html + main.css + app.js`로 나뉜 일반 CSS 파일 방식을 따로 열어 비교할 수 있게 하기 위해서입니다.
- `index-tailwind.html`을 추가했습니다.
  - 이유: `main.css` 없이 Tailwind CDN을 불러오고, UI 스타일을 HTML `class`에 직접 작성하는 방식을 따로 확인할 수 있게 하기 위해서입니다.
- `app.js`의 화면 전환에서 `hidden` 속성도 함께 갱신하도록 보강했습니다.
  - 이유: `main.css`가 없는 Tailwind class 버전에서도 `home`, `courses`, `result`, `saved` 화면이 확실히 숨김/표시되게 하기 위해서입니다.
- JS가 동적으로 만드는 저장 목록과 상세 가이드 단계에 Tailwind 유틸리티 클래스도 함께 붙였습니다.
  - 이유: Tailwind 버전에서 저장/상세가이드 내용이 생성된 뒤에도 기본적인 카드 스타일을 유지하기 위해서입니다.
- Tailwind 버전의 상세 가이드 모달에서 기본 `flex` 클래스를 제거하고, `app.js`가 열 때만 `flex`를 붙이도록 수정했습니다.
  - 이유: Tailwind 유틸리티 우선순위에 따라 `hidden`보다 `flex`가 이겨 모달이 처음부터 보이고 닫히지 않는 문제가 생길 수 있기 때문입니다.
- HTML 파일들의 `app.js` 캐시 버전을 `v=20260528`로 올렸습니다.
  - 이유: 브라우저가 이전 `app.js`를 캐시하고 있으면 수정 후에도 같은 모달 문제가 계속 보일 수 있기 때문입니다.

## 추가 작업 - 비교용 HTML 파일 제거

- `index-css.html`과 `index-tailwind.html`을 제거했습니다.
  - 이유: 사용자가 최종 파일은 `index.html`만 남기기를 요청했기 때문입니다.

## 수정 작업 - 상세 가이드 이미지 연결과 비율 보정

- 성산 코스 카드에만 `data-guide-image="course-img/성산.png"`를 연결했습니다.
  - 이유: 다른 코스의 `코스 상세 가이드 보기`를 눌렀을 때 성산 이미지가 계속 재사용되지 않게 하기 위해서입니다.
- `app.js`가 카드에 `data-guide-image`가 없을 때 기존 모달 이미지를 재사용하지 않고 기본 이미지로 되돌리도록 수정했습니다.
  - 이유: 코스별 이미지가 없는 상태에서는 이전 코스 이미지가 남아 있으면 잘못된 상세 가이드로 보이기 때문입니다.
- `.guide-detail-image`의 `min-height` 강제값을 제거하고 `height: auto`, `width: min(100%, 820px)`로 바꿨습니다.
  - 이유: 성산 상세 가이드 PNG처럼 세로가 긴 이미지의 원본 비율이 깨지지 않게 하기 위해서입니다.
- 최신 `index.html` 복구 과정에서 빠진 성산 카드의 `data-guide-image`를 다시 추가했습니다.
  - 이유: 성산 상세 가이드 버튼을 누를 때 JS가 기본 이미지로 되돌아가 성산 이미지가 보이지 않는 문제가 있었기 때문입니다.
- 상세 가이드 단계 영역이 코스 단계 개수에 맞춰 칸 수를 바꾸도록 `--guide-step-count` CSS 변수를 추가했습니다.
  - 이유: 기존 CSS가 4개 코스 단계 기준으로 고정되어 3개 코스에서 빈 칸처럼 보이는 문제가 있었기 때문입니다.
- 상세 가이드 단계 사이 화살표는 JS 노드가 아니라 CSS 표시로 바꿨습니다.
  - 이유: 화살표 요소가 그리드 칸을 차지하면 카드 개수 계산이 다시 틀어질 수 있기 때문입니다.
- 상세 가이드 이미지 영역의 흰색 배경, 테두리, 패딩, 그림자를 제거했습니다.
  - 이유: 완성된 상세 가이드 PNG 뒤에 별도의 흰색 박스가 한 겹 더 보여 어색했기 때문입니다.

## 수정 작업 - courses/result 코스 목록 동기화

- `result` 화면에 빠져 있던 `홍제천 산책 데이트`, `연남동 산책 데이트`, `연남동 이색 데이트` 카드를 추가했습니다.
  - 이유: `courses` 화면과 `result` 화면에서 보여주는 코스 목록이 달라 사용자가 같은 코스를 찾기 어려웠기 때문입니다.
- 추가한 `result` 카드에는 기존 결과 카드와 같은 `나만의 코스 저장`, `코스 상세 가이드 보기` 버튼을 붙였습니다.
  - 이유: 어느 화면에서 보든 같은 코스를 저장하고 상세 가이드로 이어갈 수 있게 하기 위해서입니다.
- `courses` 화면에서 특정 코스의 `자세히 보기`를 누르면 `result` 화면의 같은 코스 카드 위치로 이동하도록 보강했습니다.
  - 이유: 모든 코스가 `result`에 들어간 뒤에도 사용자가 선택한 코스를 바로 찾을 수 있게 하기 위해서입니다.

## 수정 작업 - 둘러보기 코스 상세보기 바로 연결

- `courses` 화면의 버튼 문구를 `자세히 보기`에서 `코스 상세보기`로 변경했습니다.
  - 이유: 버튼을 눌렀을 때 결과 목록으로 이동하는 것이 아니라 해당 코스의 상세 가이드를 바로 확인하는 동작을 더 정확히 보여주기 위해서입니다.
- `courses` 화면의 버튼 연결을 `data-route="result"`에서 `data-guide-open`으로 변경했습니다.
  - 이유: 둘러보기 화면에서 사용자가 코스를 고르면 `result` 화면을 거치지 않고 바로 상세 가이드 모달을 열 수 있게 하기 위해서입니다.
- 홍제천 카드에도 `data-guide-image="course-img/홍제천.png"`를 맞춰 추가했습니다.
  - 이유: 둘러보기 화면에서 바로 상세 모달을 열 때도 홍제천 상세 이미지가 빠지지 않게 하기 위해서입니다.

## 수정 작업 - courses/result 카드 구조 완전 동기화

- `courses` 화면의 각 코스 카드에 `result` 화면과 같은 `나만의 코스 저장`, `코스 상세 가이드 보기` 버튼과 저장 상태 문구를 추가했습니다.
  - 이유: 둘러보기 화면의 코스 카드가 결과 화면의 코스 카드와 같은 기능과 구조를 갖도록 하기 위해서입니다.
- 기존 `courses` 화면의 단일 `코스 상세보기` 버튼은 제거했습니다.
  - 이유: `result` 화면 카드와 동일한 액션 구성을 유지하기 위해서입니다.

## 수정 작업 - 코스 카드 버튼 높이 통일

- `.action-btn`에 고정 최소 높이와 가운데 정렬을 적용하고, `.action-stack`을 동일한 2행 구조로 고정했습니다.
  - 이유: `나만의 코스 저장`, `코스 상세 가이드 보기` 버튼 높이가 코스마다 달라 보이지 않게 하기 위해서입니다.
- `.card-top`, `.course-path`, `.action-stack` 배치를 보정해 버튼 묶음이 카드 하단으로 정렬되도록 했습니다.
  - 이유: 코스 제목, 설명, 단계 개수가 달라도 버튼 위치가 흔들리지 않게 하기 위해서입니다.

## 추가 작업 - 망원 포은로 5~6만원대 코스 카드 추가

- `courses` 화면과 `result` 화면에 `망원 포은로 데이트` 코스를 같은 구조로 추가했습니다.
  - 이유: 사용자가 정리한 `가가77페이지 → 헤키 망원 → 티노마드 → 포은로 산책` 코스를 기존 코스 카드 형식과 동일하게 보여주기 위해서입니다.
- 새 코스는 `총 49,000~58,000원`, `약 4~5시간`, `도보 위주`로 표시했습니다.
  - 이유: 5~6만원대 추천 조합 기준 금액과 소요 시간을 카드에서 바로 확인할 수 있게 하기 위해서입니다.
- 코스 단계 가격처럼 긴 텍스트가 카드 밖으로 튀어나오지 않도록 `.node-name`, `.node-price` 줄바꿈 처리를 보강했습니다.
  - 이유: `31,000~36,000원` 같은 긴 가격 범위도 기존 카드 레이아웃 안에서 자연스럽게 보이게 하기 위해서입니다.

## 추가 작업 - 망원 3~4만원대 대표 코스 카드 추가

- `courses` 화면과 `result` 화면에 `망원 감성 데이트` 코스를 같은 구조로 추가했습니다.
  - 이유: 사용자가 확정한 `제로스페이스 → 딥블루레이크 → 망원시장 → 네컷사진 → 망리단길 산책` 코스를 기존 코스 카드 형식과 동일하게 보여주기 위해서입니다.
- 새 코스는 추천 조합 기준으로 `총 32,000~40,000원`, `약 4~5시간`, `도보 위주`로 표시했습니다.
  - 이유: 3~4만원대에 맞춰 이용하려면 제로스페이스는 구경 위주, 카페와 시장 먹거리는 가볍게 잡는 구성이 적절하기 때문입니다.

## 수정 작업 - 상세 가이드 문구와 이동 방식 표시 보정

- `card-info`의 이동 방식 표시를 이모지 기반에서 `약 4~5시간 · 도보 위주` 같은 텍스트 기반으로 변경했습니다.
  - 이유: 일부 환경에서 `🚶`, `👣` 이모지가 깨진 문자처럼 보일 수 있기 때문입니다.
- `app.js`의 이동 방식 파싱을 이모지에 의존하지 않고 깨진 문자와 구분점을 정리해서 읽도록 보강했습니다.
  - 이유: 상세 가이드의 `이동 방식` 영역에 `� 도보 위주`처럼 표시되는 문제를 막기 위해서입니다.
- 상세 가이드 단계 설명을 각 HTML 카드의 `.path-node`에 넣은 `data-step-desc` 값에서 읽도록 추가했습니다.
  - 이유: `HTML 카드에 입력된 코스 단계입니다.` 같은 임시 문구 대신 코스별 장소 설명을 HTML에서 직접 관리할 수 있게 하기 위해서입니다.

## 수정 작업 - 연남/서교 상세 가이드 단계 설명 반영

- 사용자가 승인한 연남동 3~4만원대, 연남동 5~6만원대, 서교동 3~4만원대, 서교동 5~6만원대 코스의 단계별 설명을 `data-step-desc`에 반영했습니다.
  - 이유: 상세 가이드에서 각 장소의 역할과 분위기를 바로 이해할 수 있게 하기 위해서입니다.
- 잘못 들어간 `data-step-desc` 속성을 전체 정리한 뒤 장소명 기준으로 다시 연결했습니다.
  - 이유: 같은 `.path-node` 구조가 반복되어 엉뚱한 카드에 설명이 붙는 문제를 바로잡기 위해서입니다.

## 수정 작업 - 서교동 코스 이름 변경

- `서교동 데이트(3~4만원)`을 `서교동 디자인 데이트`로 변경했습니다.
  - 이유: KT&G 상상마당, 디자인 소품, 카페 흐름이 코스 성격을 더 잘 보여주기 때문입니다.
- `서교동 데이트(5~6만원)`을 `서교동 무드 데이트`로 변경했습니다.
  - 이유: 소품샵, 피자, 칵테일로 이어지는 분위기 있는 데이트 흐름을 이름에서 바로 알 수 있게 하기 위해서입니다.

## 수정 작업 - 코스 상세 이미지 연결

- `course-img` 폴더에 추가된 이미지 파일명을 기준으로 각 코스 카드의 `data-guide-image`를 연결했습니다.
  - 이유: `코스 상세 가이드 보기`를 눌렀을 때 코스별 상세 이미지가 자동으로 바뀌게 하기 위해서입니다.
- 망원 감성, 망원 포은로, 연남 산책, 연남 이색, 서교동 디자인, 서교동 무드 코스에 상세 이미지를 추가했습니다.
  - 이유: 기존에는 성산/홍제천 일부 카드만 이미지가 연결되어 있어 다른 코스가 기본 이미지로 보일 수 있었기 때문입니다.
- 홍제천 코스 이미지 파일명을 `홍계천.png`에서 `홍제천.png`로 변경하고 HTML 경로도 `course-img/홍제천.png`로 맞췄습니다.
  - 이유: 코스 이름과 이미지 파일명을 같은 표기로 맞춰 관리하기 쉽게 하기 위해서입니다.
- `courses` 화면의 망원 포은로 코스 단계 설명을 `result` 화면과 같은 문구로 맞췄습니다.
  - 이유: 두 화면의 카드 구조와 상세 가이드 내용이 완전히 동일하게 유지되도록 하기 위해서입니다.

## 수정 작업 - PR 충돌 해결

- 최신 `origin/main`을 병합하면서 발생한 `app.js`, `index.html` 충돌을 해결했습니다.
  - 이유: PR 브랜치가 최신 main 위에서 병합 가능하도록 만들기 위해서입니다.
- 충돌 해결 시 코스 정보를 `HTML`에 직접 두고 상세 가이드에는 이미지 파일을 연결하는 현재 구조를 유지했습니다.
  - 이유: 이 프로젝트에서는 코스 정보를 별도 데이터 파일이나 지도 API가 아니라 HTML 카드와 이미지로 관리하기로 했기 때문입니다.
- 병합 과정에서 추가된 `data.js`는 제거했습니다.
  - 이유: 현재 구조와 섞이면 같은 코스 데이터를 HTML과 JS 데이터 파일에서 이중 관리하게 되기 때문입니다.

## 수정 작업 - main의 data.js 구조 복구

- 최신 `main`에서 사용하던 `data.js` 기반 코스 데이터 구조를 다시 반영했습니다.
  - 이유: `main`은 `COURSE_DATA`를 기준으로 코스 카드를 렌더링하므로, PR도 이 구조를 유지하는 편이 충돌과 중복 관리를 줄일 수 있기 때문입니다.
- 기존에 HTML에 직접 들어가 있던 코스 카드 정보는 `data.js`의 코스 객체와 장소 목록으로 옮겼습니다.
  - 이유: 새 코스 추가 시 HTML 카드 전체를 복사하지 않고 데이터만 추가할 수 있게 하기 위해서입니다.
- `app.js`가 `COURSE_DATA`를 읽어 `home`, `courses`, `result` 화면의 코스 카드를 생성하도록 변경했습니다.
  - 이유: 최신 `main`의 자동 렌더링 방식은 유지하면서, 기존 상세 가이드 이미지와 저장 기능도 계속 사용할 수 있게 하기 위해서입니다.
- `index.html`은 코스 카드가 들어갈 빈 컨테이너와 상세 가이드 모달만 두는 구조로 정리했습니다.
  - 이유: HTML과 데이터가 이중으로 관리되지 않게 하기 위해서입니다.

## 수정 작업 - result 화면 지도 API 연결 복구

- `index.html`에 카카오 지도 SDK와 `result` 화면의 `#map` 지도 영역을 다시 연결했습니다.
  - 이유: 최신 `main`에서 제공하던 결과 화면 지도 API 기능을 PR에서도 유지하기 위해서입니다.
- `app.js`에 `COURSE_DATA`의 장소 목록을 기준으로 지도 마커와 경로선을 그리는 `initMap` 흐름을 복구했습니다.
  - 이유: `data.js` 기반 코스 구조를 유지하면서 선택한 코스의 장소들이 지도에 표시되도록 하기 위해서입니다.
- 상세 가이드 모달은 기존처럼 코스별 이미지(`guideImage`)를 사용하도록 유지했습니다.
  - 이유: 상세 가이드는 지도 API가 아니라 준비된 이미지 한 장을 연결하기로 한 요구사항을 지키기 위해서입니다.

## 추가 작업 - 합정동/연남동 코스 4개 추가

- `data.js`에 `합정동 골목 산책 데이트`, `합정동 편집샵 무드 데이트`, `연남동 토마 골목 데이트`, `연남동 오버딥 무드 데이트` 코스를 추가했습니다.
  - 이유: 사용자가 제공한 합정동 3~4만원대/5~6만원대 코스와 연남동 3~4만원대/5~6만원대 코스를 기존 코스와 함께 모두 보여주기 위해서입니다.
- 기존 연남동 코스 2개는 삭제하거나 덮어쓰지 않고 그대로 유지했습니다.
  - 이유: 사용자가 “전부 추가”를 승인했으므로 기존 코스와 새 코스를 함께 남기는 방향이 맞기 때문입니다.
- 새 코스의 장소에는 지도 검색용 `mapName`을 함께 넣었습니다.
  - 이유: 카드에 보이는 짧은 장소명만으로 지도 API가 애매하게 검색될 수 있어, 지도 검색에는 더 구체적인 이름을 사용할 수 있게 하기 위해서입니다.
- 새 코스의 `guideImage`는 임시로 기존 대표 일러스트(`웹 사이트 메인화면 일러.PNG`)에 연결했습니다.
  - 이유: 아직 합정동/새 연남동 전용 상세 가이드 이미지 파일이 없어서, 빈 이미지나 깨진 이미지 대신 기존 이미지로 안전하게 표시하기 위해서입니다.

## 수정 작업 - 기존 연남동 코스 가격 반영

- `연남동 산책 데이트`의 `음무아 연남점` 가격을 `약 27,000원`으로 반영하고, 카페 단계를 `카페 공명 연남점` / `약 15,000원`으로 수정했습니다.
  - 이유: 사용자가 제공한 연남동 3~4만원대 코스의 실제 음식점 가격을 카드와 상세 가이드에 맞추기 위해서입니다.
- `연남동 이색 데이트`의 `연남토마 본점`, `도토리 캐리커쳐`, `연남동집` 가격을 각각 `약 33,000원`, `약 14,000원`, `약 13,000~15,000원`으로 반영했습니다.
  - 이유: 사용자가 제공한 연남동 5~6만원대 코스 가격 정보를 기존 코스 단계에 정확히 표시하기 위해서입니다.
- 가격을 수정한 장소에는 지도 검색용 `mapName`도 함께 보강했습니다.
  - 이유: 표시명과 지도 검색어를 분리해 지도 API 검색 결과가 더 안정적으로 잡히게 하기 위해서입니다.

## 수정 작업 - 코스 가격대 표기 통일

- `망원 감성 데이트`, `망원 포은로 데이트`, `서교동 디자인 데이트`, `서교동 무드 데이트`의 상단 가격 표시를 `총 3~4만원` 또는 `총 5~6만원` 형태로 통일했습니다.
  - 이유: 카드마다 `총 41,000원`, `총 32,000~40,000원`처럼 서로 다른 표기 방식이 섞여 있어, 사용자가 가격대를 한눈에 비교하기 어렵기 때문입니다.
- 장소별 세부 금액은 기존처럼 `약 27,000원`, `약 13,000~15,000원` 형태를 유지했습니다.
  - 이유: 코스 전체 가격대와 장소별 예상 지출은 역할이 달라, 장소 단계에서는 구체적인 금액이 더 유용하기 때문입니다.

## 수정 작업 - 상세 가이드 화면 분리

- `코스 상세 가이드 보기`를 기존 모달 방식에서 `guide` 전용 화면으로 변경했습니다.
  - 이유: 모달 안에서 마우스 휠을 내릴 때 뒤에 있던 `result` 또는 `courses` 화면까지 같이 스크롤되는 문제를 막기 위해서입니다.
- `index.html`의 상세 가이드 구조를 `data-screen="guide"` 섹션 안으로 옮겼습니다.
  - 이유: `home`, `courses`, `result`, `saved`와 같은 SPA 화면 전환 방식으로 상세 가이드를 보여주기 위해서입니다.
- `app.js`의 상세 가이드 열기 동작을 `openGuideScreen()`으로 바꿔, 선택한 코스 정보를 채운 뒤 `guide` 화면으로 전환하도록 수정했습니다.
  - 이유: 상세 가이드가 별도 화면처럼 동작하면서도 코스별 제목, 예산, 단계, 이미지는 기존처럼 자동으로 바뀌어야 하기 때문입니다.
- `main.css`에서 fixed 모달 오버레이 스타일을 제거하고 일반 페이지 섹션 스타일을 추가했습니다.
  - 이유: 상세 가이드가 독립 화면의 자연스러운 페이지 스크롤을 사용하게 하기 위해서입니다.

## 수정 작업 - 상세 가이드 단계 아이콘/장소명 배치 변경

- 상세 가이드의 각 장소 단계에 `guide-stage-heading` 구조를 추가해 아이콘과 장소명을 한 줄에 배치했습니다.
  - 이유: 장소명이 아이콘 아래에 따로 떨어져 보이는 것보다, 아이콘 옆에서 바로 읽히는 구성이 단계 정보를 빠르게 파악하기 좋기 때문입니다.
- `.guide-stage-icon` 크기를 기존 44px에서 58px로 키우고 아이콘 글자 크기도 함께 키웠습니다.
  - 이유: 상세 가이드 화면에서 장소별 아이콘이 더 잘 보이도록 하기 위해서입니다.
- 상세 가이드 단계 카드의 정렬을 왼쪽 기준으로 보정했습니다.
  - 이유: 아이콘 옆 장소명, 아래 설명, 가격이 한 흐름으로 읽히게 하기 위해서입니다.

## 수정 작업 - 상세 가이드 본문 2단 배치

- `index.html`의 상세 가이드 본문에 `guide-body` 래퍼를 추가해 코스 단계 영역과 상세 이미지 영역을 같은 묶음으로 정리했습니다.
  - 이유: 데스크톱 화면에서 왼쪽에는 장소별 코스 단계, 오른쪽에는 상세 이미지가 나란히 보이도록 배치하기 위해서입니다.
- `main.css`에서 데스크톱 기준 `guide-body`를 2단 grid로 바꾸고, 코스 단계 카드는 왼쪽 세로 흐름으로 유지했습니다.
  - 이유: 상세 이미지가 아래로 밀려 내려가지 않고 코스 설명과 동시에 보이게 하기 위해서입니다.
- 상세 이미지 영역은 오른쪽 컬럼 안에서 너비에 맞게 표시되도록 조정하고, 좁은 화면에서는 기존처럼 위아래로 쌓이게 유지했습니다.
  - 이유: PC에서는 이미지 확인이 편하고, 모바일에서는 화면 폭이 좁아도 내용이 깨지지 않게 하기 위해서입니다.

## 수정 작업 - 상세 가이드 참고 이미지형 레이아웃 반영

- 상세 가이드 구조를 `왼쪽: 요약 카드와 장소 타임라인`, `오른쪽: 큰 상세 이미지` 형태로 다시 정리했습니다.
  - 이유: 사용자가 보낸 참고 이미지처럼 설명과 이미지가 한 화면에서 동시에 보이는 구성이 더 직관적이기 때문입니다.
- `app.js`의 상세 단계 생성 구조를 바꿔 아이콘 옆에 장소명과 가격이 함께 나오고, 설명은 그 아래에 이어지도록 수정했습니다.
  - 이유: 장소별 정보가 카드 박스처럼 분리되기보다 세로 동선 안내처럼 읽히게 하기 위해서입니다.
- `main.css`에서 상세 단계 카드의 테두리와 흰 배경을 제거하고, 단계 사이에는 아래쪽 화살표가 보이도록 조정했습니다.
  - 이유: 참고 이미지의 타임라인 느낌을 살리고 코스 순서를 더 자연스럽게 보여주기 위해서입니다.
- 오른쪽 상세 이미지는 넓은 비율의 이미지 영역을 채우도록 조정했습니다.
  - 이유: 지도나 코스 이미지가 오른쪽에서 크게 보이며, 왼쪽 설명과 시각적으로 균형을 맞추도록 하기 위해서입니다.

## 수정 작업 - 상세 가이드 장소 카드 구분감 강화

- 상세 가이드의 각 장소 단계에 연한 배경색, 테두리, 둥근 모서리, 그림자를 추가했습니다.
  - 이유: 장소별 정보가 서로 붙어 보이지 않고 하나의 단계 단위로 구분되게 하기 위해서입니다.
- 장소 단계 사이의 간격을 넓히고 화살표 위치를 카드 사이 중앙으로 조정했습니다.
  - 이유: 코스 순서는 유지하면서도 각 장소 카드가 답답하게 붙어 보이지 않도록 하기 위해서입니다.
- 짝수 번째 장소 카드에는 아주 옅게 다른 배경색을 적용했습니다.
  - 이유: 같은 형태의 카드가 반복될 때 시각적으로 더 쉽게 구분되도록 하기 위해서입니다.

## 수정 작업 - 상세 가이드 지도 이미지 간격 조정

- 데스크톱 상세 가이드에서 왼쪽 장소 카드 영역과 오른쪽 지도 이미지 영역 사이의 간격을 더 넓혔습니다.
  - 이유: 두 영역이 너무 붙어 보여 답답하게 느껴지는 문제를 줄이고, 설명 영역과 이미지 영역을 더 명확하게 분리하기 위해서입니다.
- 간격은 고정값이 아니라 화면 너비에 따라 `clamp()`로 조절되도록 했습니다.
  - 이유: 큰 화면에서는 충분히 여백을 주고, 좁은 화면에서는 레이아웃이 과하게 벌어지지 않게 하기 위해서입니다.

## 수정 작업 - 상세 가이드 요약 카드 양쪽 모서리 통일

- 상세 가이드 상단 요약 카드의 왼쪽 모서리도 오른쪽과 같은 둥근 모서리로 변경했습니다.
  - 이유: 한쪽만 둥글게 보여 어색했던 부분을 양쪽 모두 같은 형태로 맞추기 위해서입니다.

## 수정 작업 - 상세 가이드 상단 요약 전체 폭 배치

- 상세 가이드의 상단 요약 카드를 왼쪽 코스 설명 영역 밖으로 빼서 상세 화면 전체 폭을 쓰도록 구조를 변경했습니다.
  - 이유: 사용자가 보낸 참고 이미지처럼 제목, 예산, 시간, 이동 방식, 닫기 버튼이 한 줄의 상단 바처럼 보이게 하기 위해서입니다.
- 상단 요약 아래에서만 `왼쪽 장소 카드 / 오른쪽 지도 이미지` 2단 레이아웃이 시작되도록 정리했습니다.
  - 이유: 지도 이미지가 상단 요약 카드와 같은 높이에서 붙어 보이지 않고, 본문 영역에서 독립적으로 보이게 하기 위해서입니다.
- 본문 2단 레이아웃의 컬럼 비율과 간격을 다시 조정했습니다.
  - 이유: 참고 이미지처럼 왼쪽 안내 영역과 오른쪽 지도 이미지가 균형 있게 보이도록 하기 위해서입니다.

## 수정 작업 - 상세 가이드 영역 간 여백 확대

- 상세 가이드 화면 상단과 요약 카드 사이의 여백을 키웠습니다.
  - 이유: 사이트 헤더 바로 아래에 상세 가이드 카드가 붙어 보여 답답한 느낌을 줄이기 위해서입니다.
- 요약 카드와 아래 본문 영역 사이의 간격을 더 넓혔습니다.
  - 이유: 상단 요약 정보와 장소 카드/지도 영역이 서로 다른 정보 그룹으로 읽히게 하기 위해서입니다.
- 데스크톱에서 장소 카드 영역과 지도 이미지 영역 사이의 간격을 더 크게 조정했습니다.
  - 이유: 왼쪽 아이템들과 오른쪽 지도가 너무 가까워 보이는 문제를 줄이기 위해서입니다.

## 수정 작업 - 상세 가이드 비율 및 지도 테두리 보강

- 상세 가이드 전용 컨테이너 폭을 넓히고, 본문을 왼쪽 코스 카드 약 44%, 오른쪽 지도 이미지 약 56% 비율로 다시 조정했습니다.
  - 이유: 기존에는 왼쪽/오른쪽 영역의 비율이 어긋나 보여 전체 구성이 불안정하게 느껴졌기 때문입니다.
- 지도 이미지 영역에 두꺼운 테두리, 연한 배경, 둥근 모서리, 그림자를 추가했습니다.
  - 이유: 지도 부분이 배경과 구분되지 않고 떠 보이는 문제를 줄이고, 명확한 이미지 프레임처럼 보이게 하기 위해서입니다.
- 데스크톱 지도 이미지 높이를 줄이고 안정적인 범위 안에서 반응형으로 조정되게 했습니다.
  - 이유: 지도만 과하게 커 보이지 않고 장소 카드 묶음과 시각적인 균형을 맞추기 위해서입니다.

## 수정 작업 - 상세 가이드 헤더와 코스 카드 왼쪽 정렬

- 데스크톱 상세 가이드에서 코스 카드 목록에 들어가 있던 좌우 여백을 제거했습니다.
  - 이유: 상단 요약 헤더의 왼쪽 끝과 첫 번째 장소 카드의 왼쪽 끝이 어긋나 보였기 때문입니다.

## 수정 작업 - 상세 가이드 헤더 테두리 추가

- 상세 가이드 상단 요약 헤더에 브랜드 색감의 연한 테두리와 그림자를 추가했습니다.
  - 이유: 지도 이미지와 장소 카드처럼 헤더도 하나의 정보 영역으로 명확하게 구분되도록 하기 위해서입니다.

## 수정 작업 - result 화면 코스 카드 내부 스크롤

- `result` 화면의 지도 높이를 `--result-panel-height` 변수로 관리하도록 변경했습니다.
  - 이유: 지도 UI 높이와 왼쪽 코스 카드 영역 높이를 같은 기준으로 맞추기 위해서입니다.
- 데스크톱에서 왼쪽 `result-sidebar` 높이를 지도와 동일한 600px로 제한하고, 추천 코스 카드 목록만 내부 스크롤되도록 수정했습니다.
  - 이유: 추천 카드가 지도 UI보다 길어질 때 페이지 전체가 길게 밀리지 않고, 마지막 카드가 일부 보인 상태에서 스크롤로 이어지게 하기 위해서입니다.
- 카드 목록 스크롤바를 브랜드 색감에 맞게 얇게 조정했습니다.
  - 이유: 내부 스크롤이 생겨도 UI가 튀지 않고 기존 디자인과 어울리게 하기 위해서입니다.

## 수정 작업 - result 화면 카드 기본 표시 복구

- `result` 화면에 추가했던 코스 카드 내부 스크롤 제한을 제거했습니다.
  - 이유: 사용자가 기존 카드 형태처럼 자연스럽게 전체 카드 목록이 이어져 보이는 방식을 원했기 때문입니다.
- 지도 높이는 모바일 350px, 데스크톱 600px의 기존 고정 높이 방식으로 되돌렸습니다.
  - 이유: 내부 스크롤용 높이 변수 없이도 기존 레이아웃이 안정적으로 보이도록 하기 위해서입니다.

## 수정 작업 - 코스 카드 저장 버튼 하트 아이콘화

- `courses`와 `result` 화면의 긴 `나만의 코스 저장` 버튼을 카드 우측 상단의 `♡` 아이콘 버튼으로 변경했습니다.
  - 이유: 사용자가 보낸 예시 이미지처럼 카드 안에서 저장 기능이 더 가볍고 직관적으로 보이게 하기 위해서입니다.
- 저장된 코스는 하트가 `♥`로 바뀌고, `aria-pressed`와 `aria-label`도 저장 상태에 맞게 갱신되도록 유지했습니다.
  - 이유: 기존 저장 기능과 접근성 상태는 유지하면서 시각 표현만 아이콘 중심으로 바꾸기 위해서입니다.
- 하단 액션 영역은 `코스 상세 가이드 보기` 버튼만 남도록 정리했습니다.
  - 이유: 저장 버튼이 상단 아이콘으로 이동했으므로 카드 하단 버튼 구성을 더 단순하게 만들기 위해서입니다.

## 수정 작업 - 저장 버튼 토글 및 상태 문구 자동 숨김

- 이미 저장된 코스의 하트 저장 버튼을 다시 누르면 해당 코스가 저장 목록에서 삭제되도록 변경했습니다.
  - 이유: 같은 버튼으로 저장과 저장 해제를 모두 처리하는 방식이 하트 아이콘 UI에 더 자연스럽기 때문입니다.
- 저장 상태 문구가 표시된 뒤 5초가 지나면 자동으로 사라지도록 타이머를 추가했습니다.
  - 이유: `나만의 데이트 코스에 저장했어요.` 같은 안내 문구가 화면에 계속 남아 UI를 지저분하게 만들지 않도록 하기 위해서입니다.
- 카드별 안내 문구 타이머를 따로 관리하도록 했습니다.
  - 이유: 여러 카드에서 저장 버튼을 눌러도 이전 타이머가 다른 카드의 문구를 잘못 지우지 않게 하기 위해서입니다.

## 수정 작업 - 저장 상태 문구 페이드 효과 추가

- 저장 상태 문구에 `is-visible` 클래스를 사용해 서서히 나타나고 서서히 사라지는 효과를 추가했습니다.
  - 이유: 안내 문구가 갑자기 나타나거나 사라지지 않고 더 부드럽게 보이도록 하기 위해서입니다.
- 문구가 5초 동안 보인 뒤 450ms 동안 서서히 사라지고, 사라진 뒤 텍스트가 비워지도록 타이머 로직을 조정했습니다.
  - 이유: 화면에서는 자연스럽게 사라지면서도, 사라진 후에는 불필요한 텍스트가 남지 않게 하기 위해서입니다.

## 수정 작업 - result 화면 데스크톱/모바일 스크롤 방식 분리

- 데스크톱 `result` 화면에서 추천 코스 카드 영역과 `나만의 데이트 코스` 영역의 높이를 지도 높이 600px에 맞추고, 내용이 넘치면 각 영역 안에서 세로 스크롤되도록 변경했습니다.
  - 이유: 왼쪽 카드와 오른쪽 저장 코스가 지도 UI보다 길게 내려가 화면 균형이 깨지는 문제를 줄이기 위해서입니다.
- 모바일 `result` 화면에서는 추천 코스 카드와 저장 코스 목록을 가로 스와이프 카드 레일로 변경했습니다.
  - 이유: 사용자가 보낸 이미지처럼 가운데 카드가 중심이 되고 양옆 카드가 일부 보이는 느낌으로 탐색할 수 있게 하기 위해서입니다.
- 모바일 가로 스와이프에는 `scroll-snap-type`을 적용했습니다.
  - 이유: 손가락으로 넘겼을 때 카드가 어중간한 위치에 멈추지 않고 카드 단위로 보기 좋게 맞춰지도록 하기 위해서입니다.

## 수정 작업 - result 카드 버튼/지도 테두리/저장 카드 크기 보정

- `result` 화면의 추천 코스 카드에 최소 높이를 지정해 `코스 상세 가이드 보기` 버튼이 카드 안에서 보이도록 보정했습니다.
  - 이유: 카드 높이가 짧아지면서 하단 상세 보기 버튼이 눈에 띄지 않는 문제가 있었기 때문입니다.
- 지도 UI 테두리를 2px 브랜드 색감 테두리와 그림자로 강화했습니다.
  - 이유: 지도 영역이 주변 흰 배경과 더 명확하게 구분되도록 하기 위해서입니다.
- 데스크톱 `나만의 데이트 코스` 목록에 `grid-auto-rows: max-content`와 `align-content: start`를 적용했습니다.
  - 이유: 저장 코스가 1개만 있을 때 카드가 세로로 과하게 늘어나지 않고, 여러 개 저장했을 때와 같은 기본 카드 크기를 유지하게 하기 위해서입니다.

## 수정 작업 - result 데이트 비중 제거 및 영역 테두리 분리

- `result` 화면에서 `데이트 비중` 영역이 남아 있지 않은지 확인했습니다.
  - 이유: 해당 기능은 빠질 예정이라 결과 화면에서 추천 카드, 지도, 저장 코스에 집중되도록 하기 위해서입니다.
- `result` 화면의 추천 코스, 지도, 나만의 데이트 코스 영역에 동일한 패널 테두리와 배경을 적용했습니다.
  - 이유: 각 영역의 경계가 눈에 보이도록 분리해 화면 구조를 더 명확하게 만들기 위해서입니다.
- `result` 화면 안의 저장 코스 목록은 바깥 패널 테두리만 보이도록 내부 `saved-courses` 박스 스타일을 투명하게 정리했습니다.
  - 이유: 저장 영역 안에 테두리가 중복으로 생겨 답답해 보이지 않게 하기 위해서입니다.
- 데스크톱 결과 화면에서 지도 이미지가 패널 안쪽 높이에 맞춰 들어가도록 지도 컨테이너 높이를 보정했습니다.
  - 이유: 패널 테두리와 패딩을 추가한 뒤에도 지도 영역이 넘치지 않고 정돈되어 보이도록 하기 위해서입니다.

## 수정 작업 - 모바일 result 카드 양옆 미리보기

- 모바일 `result` 화면의 추천 코스 카드 레일에 카드 폭 변수와 양옆 여백 변수를 추가했습니다.
  - 이유: 가운데 카드만 꽉 차게 보이지 않고, 이전/다음 카드가 양옆에 살짝 보여 가로로 넘길 수 있다는 느낌을 주기 위해서입니다.
- `result` 화면의 저장 코스 목록도 같은 모바일 카드 레일 규칙을 사용하도록 맞췄습니다.
  - 이유: 추천 카드와 저장 카드가 모바일에서 서로 다른 방식으로 움직이지 않게 하기 위해서입니다.
- 데스크톱에서는 기존처럼 세로 스크롤/그리드 형태가 유지되도록 모바일용 여백과 스냅 패딩을 해제했습니다.
  - 이유: 모바일 캐러셀 보정이 데스크톱 결과 화면 레이아웃을 건드리지 않게 하기 위해서입니다.
- 특정 코스로 이동할 때 모바일에서는 해당 코스 카드가 가운데로 오도록 `scrollIntoView`의 가로 정렬을 보정했습니다.
  - 이유: 카드가 왼쪽 끝에 붙지 않고 참고 이미지처럼 중심 카드와 주변 카드가 함께 보이게 하기 위해서입니다.

## 수정 작업 - 모바일 result 카드 레일 전체 폭 보정

- 모바일 `result` 화면의 카드 레일을 패널 내부 폭이 아니라 화면 전체 폭(`100vw`) 기준으로 펼치도록 변경했습니다.
  - 이유: 기존에는 패널 padding과 container padding 안에서 카드가 움직여 양옆 카드가 너무 조금만 보였기 때문입니다.
- 카드 폭을 `clamp(285px, 80vw, 340px)`로 변경했습니다.
  - 이유: 카드가 너무 좁아져 세로로 길어지는 문제를 줄이면서도, 양옆에 이전/다음 카드가 보일 공간을 남기기 위해서입니다.
- 데스크톱에서는 카드 레일 폭을 다시 `100%`로 되돌리도록 보정했습니다.
  - 이유: 모바일 전용 전체 폭 캐러셀 규칙이 데스크톱의 세로 카드 목록과 저장 코스 목록에 영향을 주지 않게 하기 위해서입니다.

## 수정 작업 - 모바일 result 카드 영역 테두리 확장

- 모바일 `result` 화면에서 추천 코스 영역과 나만의 데이트 코스 영역의 패널 폭을 `100vw`로 넓혔습니다.
  - 이유: 카드 레일은 화면 전체 폭으로 펼쳐졌는데, 바깥 테두리는 기존 컨테이너 폭에 머물러 카드가 테두리 밖으로 나가 보였기 때문입니다.
- 데스크톱에서는 두 패널의 폭과 여백을 기존 레이아웃으로 되돌리도록 별도 규칙을 추가했습니다.
  - 이유: 모바일 전용 테두리 확장이 데스크톱 3단 결과 화면에는 영향을 주지 않게 하기 위해서입니다.

## 수정 작업 - 모바일 result 영역 둥근 테두리 제거

- 모바일 `result` 화면의 추천 코스, 지도, 나만의 데이트 코스 영역 테두리 모서리를 각지게 변경했습니다.
  - 이유: 화면 전체 폭으로 넓힌 테두리에 둥근 모서리가 남아 있어, 카드가 모서리 밖으로 삐져나간 것처럼 보였기 때문입니다.
- 모바일 지도 프레임의 둥근 모서리도 제거했습니다.
  - 이유: 지도 영역도 같은 결과 화면 패널처럼 모바일에서는 화면 폭에 맞춰 정돈되어 보이게 하기 위해서입니다.
- 데스크톱에서는 기존 둥근 모서리가 유지되도록 1024px 이상 규칙에서 다시 `var(--radius-2xl)`을 적용했습니다.
  - 이유: PC 화면에서는 기존 카드형 패널 디자인이 더 자연스럽기 때문입니다.

## 수정 작업 - 모바일 지도 테두리 축소

- 모바일 `result` 화면의 지도 영역 바깥 패널 padding을 `16px`에서 `8px`로 줄이고 테두리 두께를 `1px`로 낮췄습니다.
  - 이유: 모바일에서 지도 주변 테두리와 여백이 크게 보여 지도 영역이 답답하게 느껴졌기 때문입니다.
- 모바일 지도 컨테이너 자체의 테두리도 `1px`로 줄이고 그림자를 더 약하게 조정했습니다.
  - 이유: 바깥 패널 테두리와 지도 테두리가 겹쳐 두껍게 보이는 느낌을 줄이기 위해서입니다.
- 데스크톱에서는 기존 padding과 2px 테두리를 유지하도록 1024px 이상 규칙에서 다시 복구했습니다.
  - 이유: PC 화면에서는 기존 지도 카드 프레임의 시각적 구분감이 필요하기 때문입니다.

## 수정 작업 - PC result 카드 내용 기반 높이 보정

- 데스크톱 `result` 화면의 추천 코스 카드는 고정 최소 높이 대신 내용에 맞춰 자동으로 커지도록 변경했습니다.
  - 이유: 글씨가 길어진 카드가 같은 높이에 억지로 맞춰지면서 내부 텍스트가 겹쳐 보일 수 있었기 때문입니다.
- 데스크톱 `result` 화면의 코스 단계 목록은 한 줄 고정 대신 줄바꿈이 가능하도록 보정했습니다.
  - 이유: 왼쪽 추천 카드 영역은 폭이 좁아 장소가 많거나 이름이 긴 코스에서 단계 글씨가 서로 눌려 보일 수 있기 때문입니다.
- 단계 노드와 화살표의 flex 폭을 조정했습니다.
  - 이유: 장소명은 필요한 만큼 줄바꿈되고, 화살표는 너무 넓은 공간을 차지하지 않게 하기 위해서입니다.

## 수정 작업 - 저장 코스 카드 코스 버튼 추가

- `나만의 데이트 코스`에 표시되는 저장 카드마다 초록 배경의 `코스` 버튼을 추가했습니다.
  - 이유: 저장된 코스 카드에서도 바로 상세 코스 정보를 확인할 수 있게 하기 위해서입니다.
- 저장 카드의 `코스` 버튼에 `data-guide-open`과 `data-course-id`를 연결했습니다.
  - 이유: 기존 코스 카드가 아닌 저장 카드에서도 같은 상세 가이드 열기 로직을 재사용하기 위해서입니다.
- 상세 가이드 열기 함수가 버튼의 `data-course-id`를 우선 사용하도록 보정했습니다.
  - 이유: 저장 카드처럼 `data-course-card` 구조가 없는 요소에서도 해당 코스를 정확히 찾도록 하기 위해서입니다.

## 수정 작업 - 상세 가이드 헤더 저장 버튼 추가

- 상세 가이드 화면의 상단 헤더에서 닫기 버튼 왼쪽에 `♡` 저장 버튼을 추가했습니다.
  - 이유: 상세 가이드를 보다가 바로 해당 코스를 나만의 데이트 코스에 저장할 수 있게 하기 위해서입니다.
- guide 전용 저장 버튼은 기존 카드 저장 버튼과 같은 `data-save-course` 로직을 사용하되, `guide-save` 스타일로 별도 표시했습니다.
  - 이유: 기존 카드 저장 버튼의 absolute 위치 스타일을 그대로 쓰면 상세 가이드 헤더 안에서 배치가 깨질 수 있기 때문입니다.
- 저장 버튼 상태 갱신 로직을 `updateSaveButtons()`로 분리하고, 상세 가이드 코스가 바뀔 때 guide 저장 버튼의 `courseId`와 저장 상태도 함께 갱신되도록 했습니다.
  - 이유: 어느 코스의 상세 가이드를 열어도 저장 버튼이 해당 코스의 상태를 정확히 보여주게 하기 위해서입니다.

## 수정 작업 - 상세 가이드 카드/화살표 간격 보정

- 데스크톱 상세 가이드에서 장소 카드 사이 간격을 `32px`에서 `56px`로 넓혔습니다.
  - 이유: 카드 사이에 표시되는 아래쪽 화살표가 다음 장소 카드와 살짝 겹쳐 보였기 때문입니다.
- 화살표 위치를 카드 사이 중앙에 더 안정적으로 들어오도록 조정하고 `line-height: 1`을 추가했습니다.
  - 이유: 기본 line-height 때문에 화살표 실제 박스 높이가 커져 카드 영역을 침범하는 현상을 줄이기 위해서입니다.

## 수정 작업 - 저장 코스 카드 버튼 정렬 보정

- `나만의 데이트 코스` 저장 카드 하단 footer의 정렬을 `space-between`에서 왼쪽 기준 정렬로 변경했습니다.
  - 이유: 가격, 코스, 삭제 세 항목이 균등하게 벌어져 `코스` 버튼이 카드 가운데에 떠 보였기 때문입니다.
- 가격 영역에 `margin-right: auto`를 적용해 `코스` 버튼과 `삭제` 버튼이 오른쪽에 붙어 보이도록 했습니다.
  - 이유: 사용자가 요청한 것처럼 `코스` 버튼이 `삭제` 버튼 바로 왼쪽에 배치되게 하기 위해서입니다.

## 수정 작업 - courses/saved 메인 이동 버튼 추가

- `courses` 화면 상단 제목 오른쪽에 `메인으로 돌아가기` 버튼을 추가했습니다.
  - 이유: 코스 둘러보기 화면에서 메뉴를 다시 열지 않고 바로 메인 화면으로 돌아갈 수 있게 하기 위해서입니다.
- `saved` 화면의 저장 코스 헤더 오른쪽에 개수 배지와 함께 `메인으로 돌아가기` 버튼을 추가했습니다.
  - 이유: 나만의 데이트 코스 화면에서도 같은 방식으로 메인 화면 이동을 제공하기 위해서입니다.
- 두 버튼 모두 기존 SPA 전환 방식인 `data-route="home"`을 사용하도록 연결했습니다.
  - 이유: 별도 JS 로직을 추가하지 않고 기존 화면 전환 흐름을 재사용하기 위해서입니다.

## 수정 작업 - 망원 피크닉 데이트 코스 추가

- `망원 피크닉 데이트` 코스를 새로 추가했습니다.
  - 이유: 사용자가 제공한 5만 원 내외 망원동 가성비 코스를 기존 카드/상세 가이드 데이터 구조에 맞춰 표시하기 위해서입니다.
- 코스 단계는 `망원시장 먹거리 → 당도 젤라또 → 망리단길 소품샵 → 망원한강공원 피크닉` 순서로 구성했습니다.
  - 이유: 제공된 코스 흐름을 유지하면서 카드와 상세 가이드에서 읽기 좋은 짧은 장소명으로 정리하기 위해서입니다.
- 전체 가격대는 `총 4~5만원`, 최대 예산은 `50,000원`으로 설정했습니다.
  - 이유: 사용자가 제시한 총합이 약 45,000원이어서 기존 가격대 표기 방식에 맞추기 위해서입니다.
- 지도 검색 안정성을 위해 각 장소에 `mapName`을 추가했습니다.
  - 이유: 카드에 보이는 짧은 이름과 실제 지도 검색어를 분리해 지도 API가 장소를 더 잘 찾도록 하기 위해서입니다.
- 전용 상세 이미지가 아직 없어 `course-img/망원 감성.png`를 임시로 연결했습니다.
  - 이유: 이미지가 깨지지 않게 하면서, 추후 전용 이미지가 생기면 `guideImage`만 교체할 수 있게 하기 위해서입니다.

## 수정 작업 - 상세 가이드 지도 API 연결 자리 정리

- 상세 가이드 오른쪽 영역의 기존 이미지 태그를 제거하고 `data-guide-map` 지도 마운트 영역으로 교체했습니다.
  - 이유: 이후 JS 담당자가 이미지 대신 지도 API를 연결할 예정이기 때문입니다.
- `guide-map` 요소에 현재 코스 id가 `data-course-id`로 들어가도록 `updateGuideMapPlaceholder()`를 추가했습니다.
  - 이유: JS 담당자가 현재 상세 가이드에서 어떤 코스의 지도를 그려야 하는지 쉽게 확인할 수 있게 하기 위해서입니다.
- `app.js`에서 상세 가이드 이미지 갱신 로직과 `data-guide-image` 카드 속성을 제거했습니다.
  - 이유: 더 이상 상세 가이드가 이미지 기반으로 동작하지 않게 하기 위해서입니다.
- 지도 연결용 개발 메모를 `app.js`와 `index.html` 주석으로 남겼습니다.
  - 이유: 지도 API 연결 시 사용할 DOM 훅과 코스 장소 데이터 위치를 바로 파악할 수 있게 하기 위해서입니다.

## 수정 작업 - 저장 버튼 메시지 대상 디버깅

- 상세 가이드 화면의 `♡` 저장 버튼을 눌렀을 때 저장 완료/삭제 안내 문구가 첫 번째 결과 카드에 잘못 들어갈 수 있던 부분을 보정했습니다.
  - 이유: 상세 가이드 저장 버튼은 추천 카드 내부에 있지 않아서, 기존 메시지 표시 로직이 보이지 않는 다른 카드의 안내 문구 영역을 대신 선택할 수 있었기 때문입니다.
- 저장 안내 문구는 실제 코스 카드 안의 저장 버튼을 눌렀을 때만 해당 카드 안에 표시되도록 범위를 좁혔습니다.
  - 이유: 화면 밖이나 숨겨진 카드에 안내 문구가 남는 혼선을 막고, 상세 가이드에서는 저장 상태 아이콘만 정확히 갱신되게 하기 위해서입니다.

## 수정 작업 - 안전한 미사용 코드 정리

- 코스 카드 생성 시 더 이상 읽지 않는 `data-step-desc` 속성 생성을 제거했습니다.
  - 이유: 상세 가이드는 이제 카드 DOM 속성이 아니라 `data.js`의 코스 데이터를 직접 사용하므로 중복 데이터가 필요 없기 때문입니다.
- 저장 코스를 JS로 만들 때 붙이던 Tailwind 유틸리티 클래스들을 제거하고, 프로젝트 전용 클래스만 남겼습니다.
  - 이유: 현재 페이지는 `main.css`만 불러오며 Tailwind 빌드 CSS를 사용하지 않아서 해당 유틸리티 클래스들이 실제 스타일에 영향을 주지 않기 때문입니다.
- 사용되지 않는 `guide-flow-arrow`, `--guide-step-count`, `card-purple`, `card-yellow` 관련 CSS를 제거했습니다.
  - 이유: 현재 데이터와 화면 구조에서 생성되거나 참조되지 않는 스타일이라 유지할 필요가 없기 때문입니다.
- 메인 화면 안내 문구의 `이미지` 표현을 `지도 영역` 표현으로 바꿨습니다.
  - 이유: 상세 가이드 오른쪽 영역이 이미지에서 지도 API 연결 자리로 바뀐 현재 구조와 문구를 맞추기 위해서입니다.
- `package.json`의 일부러 실패하던 `test` 더미 스크립트와 존재하지 않는 `index.js` 진입점 설정을 제거했습니다.
  - 이유: 실제 테스트가 없는 상태에서 `npm test`가 의도적으로 실패하거나, 없는 파일을 진입점으로 가리키는 혼란을 줄이기 위해서입니다.
