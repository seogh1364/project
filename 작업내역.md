# 작업내역

작업일: 2026-05-19

## 기준

사용자 요청에 따라 HTML 화면을 `index.html` 하나로 통합하고, JavaScript로 화면 섹션을 숨김/표시하는 SPA 방식으로 변경했습니다.

## 변경한 파일

### `index.html`

- `main.css` 연결을 제거하고 기존 CSS를 `<style>` 안으로 통합했습니다.
  - 이유: 화면 구현 파일을 `index.html` 하나로 줄이기 위해서입니다.
- 기존 `result.html`의 결과 화면 구조를 `index.html` 내부의 `data-screen="result"` 섹션으로 옮겼습니다.
  - 이유: 별도 페이지 이동 없이 같은 HTML 안에서 화면을 전환하기 위해서입니다.
- 기존 메인 화면을 `data-screen="home"` 섹션으로 감쌌습니다.
  - 이유: JavaScript가 현재 화면을 명확히 찾고 토글할 수 있게 하기 위해서입니다.
- `result.html`로 이동하던 버튼 동작을 제거하고, `submit` 이벤트에서 결과 섹션을 보여주도록 바꿨습니다.
  - 이유: SPA 방식에서는 페이지 이동이 아니라 상태 전환으로 화면을 교체해야 하기 때문입니다.
- `data-route`, `data-budget`, `data-budget-input`, `data-selected-budget` 속성을 추가했습니다.
  - 이유: JavaScript가 버튼, 입력값, 결과 금액 표시 영역을 안정적으로 찾게 하기 위해서입니다.
- `#home`, `#result` 해시와 `history.pushState`를 사용했습니다.
  - 이유: 새로고침하거나 브라우저 뒤로가기를 눌러도 화면 상태를 자연스럽게 유지하기 위해서입니다.
- `.screen.is-active`와 `@keyframes screenIn`을 추가했습니다.
  - 이유: 화면 전환 시 섹션이 갑자기 바뀌지 않고 부드럽게 나타나도록 하기 위해서입니다.

## 제거한 파일

### `main.css`

- 스타일을 `index.html` 내부로 통합한 뒤 제거했습니다.
  - 이유: 사용자가 `index.html` 하나만 두는 구조를 요청했기 때문입니다.

### `result.html`

- 결과 화면을 `index.html` 내부 섹션으로 옮긴 뒤 제거했습니다.
  - 이유: 결과 화면도 같은 파일 안에서 JavaScript로 토글하는 SPA 방식으로 바뀌었기 때문입니다.

## 유지한 파일

- `favicon.ico`
- `만원 데이트 로고.PNG`
- `웹 사이트 메인화면 일러.PNG`

위 파일들은 `index.html`에서 실제로 참조하는 정적 이미지/아이콘 자산이라 유지했습니다.
## 추가 작업

### 코스 상세 가이드 오버레이

- `코스 상세 가이드 보기` 버튼에 `data-guide-open` 속성을 추가했습니다.
  - 이유: JavaScript가 상세 가이드 열기 버튼을 안정적으로 찾기 위해서입니다.
- 기존 사이트 색상과 같은 크림/핑크/브라운 톤의 상세 가이드 오버레이(`data-guide-modal`)와 패널을 `index.html`에 추가했습니다.
  - 이유: 사이트 전체 색감과 맞는 별도 상세 화면이 버튼 클릭 시 위에 뜨도록 하기 위해서입니다.
- 닫기 버튼, 배경 클릭, ESC 키로 오버레이를 닫는 동작을 추가했습니다.
  - 이유: 사용자가 실수로 상세 화면을 열어도 쉽게 돌아갈 수 있게 하기 위해서입니다.
- 상세 가이드에는 예산 요약, 코스 순서, 준비물 안내를 넣었습니다.
  - 이유: 현재 예시 코스 기준으로 실제 상세 가이드 화면에 필요한 최소 정보를 보여주기 위해서입니다.
- 상세 가이드의 검은 배경과 흰 글씨를 기존 팔레트(#fff8f2, #ffffff, #ff8e8e, #ff6e87, #5d4037) 기준으로 변경했습니다.
  - 이유: 기존 화면과 상세 가이드의 색상 톤을 통일하기 위해서입니다.

## 추가 작업 - 상세 가이드 레이아웃 변경

- 상세 가이드를 상단 요약 카드, 4개 코스 단계 카드, 하단 지도 API 영역 구조로 변경했습니다.
  - 이유: 사용자가 첨부한 예시 이미지처럼 상세 가이드가 한눈에 코스 흐름과 지도 영역을 보여주도록 하기 위해서입니다.
- 노란 임시 박스 색상은 사용하지 않고 기존 크림/흰색/핑크/브라운 색상 배합을 유지했습니다.
  - 이유: 사이트 전체 색감과 상세 가이드 화면의 색상 톤을 맞추기 위해서입니다.
## 추가 작업 - JS 담당자용 연결 주석과 데이터 훅 정리

- HTML에 `data-course-list`, `data-course-card`, `data-course-id`, `data-guide-stage`, `data-guide-summary`, `data-map-target` 속성을 추가했습니다.
  - 이유: 나중에 JS 담당자가 추천 코스 데이터, 상세 가이드 데이터, 지도 API를 연결할 때 DOM 위치를 안정적으로 찾게 하기 위해서입니다.
- CSS에 `.screen.is-active`, `.guide-modal.is-open`, `data-map-target="guide-map"` 사용 목적을 설명하는 주석을 추가했습니다.
  - 이유: 어떤 클래스와 속성이 JavaScript로 토글되는 상태값인지 바로 알 수 있게 하기 위해서입니다.
- JavaScript를 `dom` 객체 중심으로 정리하고, 화면 전환, 예산 포맷, 빠른 금액 버튼, 상세 가이드 모달, 브라우저 히스토리 처리 단위로 주석을 추가했습니다.
  - 이유: 사용자가 직접 JS를 담당하지 않아도, 이후 JS 담당자가 어디를 수정해야 하는지 빠르게 파악할 수 있게 하기 위해서입니다.
- 추천 카드와 상세 가이드에는 현재 예시 코스 식별자인 `hangang-picnic` 계열 값을 붙였습니다.
  - 이유: 예시 카드 3개를 나중에 실제 코스 데이터로 바꿀 때 각 카드와 상세 가이드/지도 연결 기준을 만들기 위해서입니다.
## 추가 작업 - 나만의 데이트 코스 저장 기능

- 결과 화면의 추천 카드 아래에 `나만의 코스 저장` 버튼과 저장 상태 안내 문구를 추가했습니다.
  - 이유: 사용자가 결과 화면에서 마음에 드는 코스를 바로 저장할 수 있게 하기 위해서입니다.
- 결과 화면 사이드 영역에 `나만의 데이트 코스` 목록, 저장 개수, 빈 상태 문구, 삭제 버튼을 추가했습니다.
  - 이유: 저장한 코스를 같은 result 화면에서 확인하고 관리할 수 있게 하기 위해서입니다.
- 저장 기능은 `localStorage`를 사용하도록 구현했습니다.
  - 이유: GitHub Pages처럼 서버가 없는 정적 배포 환경에서도 개인 브라우저 안에서는 저장 기능이 동작하게 하기 위해서입니다.
- `data-save-course`, `data-save-status`, `data-saved-course-list`, `data-delete-saved-course`, `data-open-saved` 훅과 JS 주석을 추가했습니다.
  - 이유: 이후 JS 담당자가 Firebase/Supabase 같은 외부 저장소로 바꿀 때 HTML/CSS 구조는 유지하고 저장 함수만 교체할 수 있게 하기 위해서입니다.